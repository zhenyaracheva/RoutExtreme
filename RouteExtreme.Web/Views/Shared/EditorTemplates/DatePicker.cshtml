@model DateTime?

@(Html.Kendo().DateTimePickerFor(m => m).Min(DateTime.Today)
              .Culture("US")
              .Format("yyyy-MM-dd")
              .HtmlAttributes(new { @class = "form-control" }))